import 'package:flutter/material.dart';

class StylesPage extends StatelessWidget {
  const StylesPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Styles page placeholder'),
    );
  }
}
