---
phase: 01-infrastructure-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pubspec.yaml
  - analysis_options.yaml
  - LICENSE
  - README.md
  - CHANGELOG.md
  - lib/tailwind_flutter.dart
  - lib/src/tokens/colors.dart
  - lib/src/tokens/spacing.dart
  - lib/src/tokens/typography.dart
  - lib/src/tokens/radius.dart
  - lib/src/tokens/shadows.dart
  - lib/src/tokens/breakpoints.dart
  - lib/src/tokens/opacity.dart
  - lib/src/theme/tw_theme.dart
  - lib/src/theme/tw_theme_data.dart
  - lib/src/theme/tw_theme_extension.dart
  - lib/src/extensions/widget_extensions.dart
  - lib/src/extensions/text_extensions.dart
  - lib/src/extensions/context_extensions.dart
  - lib/src/extensions/edge_insets_extensions.dart
  - lib/src/styles/tw_style.dart
  - lib/src/styles/tw_variant.dart
  - lib/src/styles/tw_styled_widget.dart
  - test/tailwind_flutter_test.dart
autonomous: true
requirements:
  - INF-01
  - INF-02
  - INF-07

must_haves:
  truths:
    - "`flutter pub get` resolves dependencies without errors"
    - "`flutter analyze` reports zero warnings, zero errors, zero lints"
    - "`flutter test` runs and passes (smoke test)"
    - "`dart pub publish --dry-run` reports no blocking errors"
    - "Barrel export file exists with organized commented-out sections for all 4 tiers"
    - "All 4 subdirectories under lib/src/ contain stub files matching the PRD structure"
  artifacts:
    - path: "pubspec.yaml"
      provides: "Package metadata, SDK constraints, dependencies"
      contains: "name: tailwind_flutter"
    - path: "analysis_options.yaml"
      provides: "Strict linting configuration"
      contains: "package:very_good_analysis/analysis_options.yaml"
    - path: "lib/tailwind_flutter.dart"
      provides: "Barrel export with organized sections"
      min_lines: 15
    - path: "LICENSE"
      provides: "MIT license text"
      contains: "MIT License"
    - path: "README.md"
      provides: "Minimal README for pana scoring"
      min_lines: 5
    - path: "CHANGELOG.md"
      provides: "Changelog referencing current version"
      contains: "0.0.1-dev.1"
    - path: "test/tailwind_flutter_test.dart"
      provides: "Smoke test for flutter test to pass"
      contains: "expect"
  key_links:
    - from: "pubspec.yaml"
      to: "analysis_options.yaml"
      via: "dev_dependency very_good_analysis"
      pattern: "very_good_analysis"
    - from: "analysis_options.yaml"
      to: "package:very_good_analysis"
      via: "include directive"
      pattern: "include: package:very_good_analysis"
    - from: "lib/tailwind_flutter.dart"
      to: "lib/src/**/*.dart"
      via: "commented export directives"
      pattern: "// export 'src/"
---

<objective>
Create the complete Flutter package scaffold -- pubspec.yaml, analysis_options.yaml, LICENSE, README, CHANGELOG, barrel export, directory structure with stub files, and a smoke test.

Purpose: Establish the package skeleton that passes `flutter analyze`, `flutter test`, and `dart pub publish --dry-run` before any token code is written. This is the foundation every subsequent phase builds on.

Output: A fully valid Flutter package that can be `flutter pub get`'d, analyzed, and tested.
</objective>

<execution_context>
@/Users/nicolasdemari/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nicolasdemari/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-infrastructure-foundation/01-CONTEXT.md
@.planning/phases/01-infrastructure-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create package metadata files</name>
  <files>pubspec.yaml, analysis_options.yaml, LICENSE, README.md, CHANGELOG.md</files>
  <action>
Create the following 5 files at the project root:

**pubspec.yaml** -- Use the exact template from 01-RESEARCH.md Pattern 4 with these specifics:
- name: `tailwind_flutter`
- description: `Tailwind CSS design tokens and utility-first styling API for Flutter. Type-safe, composable, theme-aware.` (exactly this -- 96 chars, within 50-180 pana range)
- version: `0.0.1-dev.1`
- homepage: `https://github.com/user/tailwind_flutter` (placeholder per CONTEXT.md)
- repository: `https://github.com/user/tailwind_flutter`
- issue_tracker: `https://github.com/user/tailwind_flutter/issues`
- topics: tailwind, design-tokens, styling, theme, utility (locked in CONTEXT.md)
- environment sdk: `">=3.3.0 <4.0.0"` (locked)
- environment flutter: `">=3.19.0"` (locked)
- dependencies: flutter sdk only (zero third-party deps, locked)
- dev_dependencies: flutter_test sdk + `very_good_analysis: ^5.1.0`

NOTE on VGA version: CONTEXT.md locked `very_good_analysis ^10.2.0`, but CONTEXT.md also locked Dart `>=3.3.0`. These are mutually exclusive -- VGA 10.2.0 requires Dart ^3.11.0. Using VGA ^5.1.0 per research recommendation since SDK constraints take priority (they came from the PRD as non-negotiable). VGA 5.1.0 provides 190 lint rules and is compatible with Dart >=3.0.0.

**analysis_options.yaml** -- Use the exact template from 01-RESEARCH.md Pattern 3:
- include: package:very_good_analysis/analysis_options.yaml
- analyzer.language: strict-casts true, strict-inference true, strict-raw-types true
- Comment out public_member_api_docs rule (Phase 5)
- Do NOT add `exclude` for `*.g.dart` -- this package has no code generation

**LICENSE** -- Standard MIT license text with copyright `2026 Nick` (from 01-RESEARCH.md)

**README.md** -- Minimal for Phase 1, use 01-RESEARCH.md template:
- Package name as H1
- One-line description
- "Under active development" notice
- Coming soon code block
- License section

**CHANGELOG.md** -- Must reference current version (pana requirement):
- Follow Keep a Changelog format
- Include `## 0.0.1-dev.1` header (MUST match pubspec version exactly)
- List: Initial package scaffold, project structure, CI pipeline, strict lint rules

After creating all files, run `flutter pub get` to verify dependency resolution.
  </action>
  <verify>
    <automated>cd /Users/nicolasdemari/dev/tailwind_flutter && flutter pub get && flutter analyze --no-fatal-infos 2>&1 | tail -5</automated>
  </verify>
  <done>All 5 metadata files exist, `flutter pub get` resolves without error, `flutter analyze` passes with zero warnings on the metadata files</done>
</task>

<task type="auto">
  <name>Task 2: Create directory structure, stub files, barrel export, and smoke test</name>
  <files>
    lib/tailwind_flutter.dart,
    lib/src/tokens/colors.dart,
    lib/src/tokens/spacing.dart,
    lib/src/tokens/typography.dart,
    lib/src/tokens/radius.dart,
    lib/src/tokens/shadows.dart,
    lib/src/tokens/breakpoints.dart,
    lib/src/tokens/opacity.dart,
    lib/src/theme/tw_theme.dart,
    lib/src/theme/tw_theme_data.dart,
    lib/src/theme/tw_theme_extension.dart,
    lib/src/extensions/widget_extensions.dart,
    lib/src/extensions/text_extensions.dart,
    lib/src/extensions/context_extensions.dart,
    lib/src/extensions/edge_insets_extensions.dart,
    lib/src/styles/tw_style.dart,
    lib/src/styles/tw_variant.dart,
    lib/src/styles/tw_styled_widget.dart,
    test/tailwind_flutter_test.dart
  </files>
  <action>
Create 4 directories under `lib/src/` and populate with stub files, then create the barrel export and smoke test.

**Stub files** (14 files total) -- Each stub is a single-line comment describing what the file will contain and which phase implements it. Do NOT add any imports, classes, or code. Example:
```dart
// This file will contain the TwColors class with 242 color constants.
// Implemented in Phase 2.
```

Create stubs in these directories:
1. `lib/src/tokens/` -- colors.dart, spacing.dart, typography.dart, radius.dart, shadows.dart, breakpoints.dart, opacity.dart (7 files)
2. `lib/src/theme/` -- tw_theme.dart, tw_theme_data.dart, tw_theme_extension.dart (3 files)
3. `lib/src/extensions/` -- widget_extensions.dart, text_extensions.dart, context_extensions.dart, edge_insets_extensions.dart (4 files)
4. `lib/src/styles/` -- tw_style.dart, tw_variant.dart, tw_styled_widget.dart (3 files)

**Barrel export** (`lib/tailwind_flutter.dart`) -- Use 01-RESEARCH.md Pattern 1 exactly:
- NO `library` directive (legacy, actively discouraged)
- 4 commented sections: Tokens (Tier 1), Theme (Tier 1 integration), Widget Extensions (Tier 2), Style Composition (Tier 3)
- All export lines are COMMENTED OUT (prefixed with `//`)
- Each export references the correct stub file path
- Include a brief header comment explaining this is the package entry point

IMPORTANT: The barrel export must have NO uncommented export directives. All exports are commented out because the stub files have no public API yet. This avoids Pitfall 4 (empty library warnings).

However, we need at least SOMETHING exported to avoid pana's "no public API" penalty. Add a single uncommented line at the top that exports a minimal version constant file. Create `lib/src/tailwind_flutter_version.dart` with:
```dart
/// The current version of the tailwind_flutter package.
const String tailwindFlutterVersion = '0.0.1-dev.1';
```
Then in the barrel export, add one uncommented export for this file before the commented sections.

**Smoke test** (`test/tailwind_flutter_test.dart`) -- Use 01-RESEARCH.md template:
```dart
import 'package:flutter_test/flutter_test.dart';
import 'package:tailwind_flutter/tailwind_flutter.dart';

void main() {
  test('package can be imported', () {
    // Verifies the package compiles and exports are reachable.
    expect(tailwindFlutterVersion, isNotEmpty);
  });
}
```
This is better than `expect(true, isTrue)` because it actually verifies the barrel export works.

After creating all files, run `flutter analyze` and `flutter test` to confirm everything passes.
  </action>
  <verify>
    <automated>cd /Users/nicolasdemari/dev/tailwind_flutter && flutter analyze --no-fatal-infos && flutter test && dart pub publish --dry-run 2>&1 | tail -10</automated>
  </verify>
  <done>
    - 14 stub files exist across 4 directories (tokens/, theme/, extensions/, styles/)
    - lib/src/tailwind_flutter_version.dart exists with version constant
    - Barrel export has 1 uncommented export (version) + all other exports commented out in organized sections
    - Smoke test passes and actually imports the package
    - `flutter analyze` zero warnings
    - `flutter test` passes
    - `dart pub publish --dry-run` reports no blocking errors
  </done>
</task>

</tasks>

<verification>
Run the full validation suite:
```bash
flutter pub get
flutter analyze --no-fatal-infos
flutter test
dart pub publish --dry-run
```
All four commands must pass with zero errors and zero warnings. The `dart pub publish --dry-run` may show informational messages about missing example/ or low dartdoc coverage -- those are expected and addressed in Phase 5.
</verification>

<success_criteria>
1. `flutter pub get` resolves without error
2. `flutter analyze` reports zero warnings, zero errors, zero lints
3. `flutter test` passes (smoke test verifies package import)
4. `dart pub publish --dry-run` reports no blocking errors
5. Barrel export has organized commented sections matching PRD structure
6. All 14 stub files exist in correct directories
7. CHANGELOG.md references version 0.0.1-dev.1 (pana requirement)
8. README.md exists with valid content (pana requirement)
9. LICENSE file contains MIT license (pana requirement)
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure-foundation/01-01-SUMMARY.md`
</output>
