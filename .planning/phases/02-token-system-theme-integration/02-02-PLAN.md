---
phase: 02-token-system-theme-integration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/src/tokens/typography.dart
  - lib/src/tokens/radius.dart
  - lib/src/tokens/shadows.dart
  - test/src/tokens/typography_test.dart
  - test/src/tokens/radius_test.dart
  - test/src/tokens/shadows_test.dart
autonomous: true
requirements: [TOK-05, TOK-06, TOK-07, TOK-08, TOK-09, TOK-10]

must_haves:
  truths:
    - "TwFontSizes.lg.value returns 18.0 and TwFontSizes.lg.lineHeight returns the correct paired ratio"
    - "TwFontSizes.lg.textStyle returns a TextStyle with both fontSize and height set"
    - "TwFontWeights.bold == FontWeight.w700"
    - "TwLetterSpacing and TwLineHeights have correct Tailwind values"
    - "TwRadii.lg is usable as a double (value 8.0) and TwRadii.lg.all returns BorderRadius.circular(8.0)"
    - "TwShadows.md is a const List<BoxShadow> with correct offset, blur, spread, and color values"
  artifacts:
    - path: "lib/src/tokens/typography.dart"
      provides: "TwFontSize extension type, TwFontSizes, TwFontWeights, TwLetterSpacing, TwLineHeights"
      contains: "TwFontSize"
    - path: "lib/src/tokens/radius.dart"
      provides: "TwRadius extension type + TwRadii abstract final class"
      contains: "TwRadius"
    - path: "lib/src/tokens/shadows.dart"
      provides: "TwShadows abstract final class with List<BoxShadow> presets"
      contains: "TwShadows"
    - path: "test/src/tokens/typography_test.dart"
      provides: "Tests for font sizes, weights, letter spacing, line heights"
      min_lines: 80
    - path: "test/src/tokens/radius_test.dart"
      provides: "Tests for radius values + BorderRadius getters"
      min_lines: 40
    - path: "test/src/tokens/shadows_test.dart"
      provides: "Tests for shadow preset values"
      min_lines: 40
  key_links:
    - from: "lib/src/tokens/typography.dart"
      to: "TextStyle"
      via: "TwFontSize.textStyle getter"
      pattern: "TextStyle get textStyle"
    - from: "lib/src/tokens/radius.dart"
      to: "BorderRadius"
      via: "TwRadius getter methods"
      pattern: "BorderRadius get"
    - from: "lib/src/tokens/shadows.dart"
      to: "BoxShadow"
      via: "static const List<BoxShadow> members"
      pattern: "static const.*List<BoxShadow>"
---

<objective>
Implement typography tokens (font sizes with paired line-heights, font weights, letter spacing, line heights), border radius tokens with BorderRadius getters, and box shadow presets.

Purpose: These three token files cover 6 requirements (TOK-05 through TOK-10) and complete the mid-complexity tier of design tokens.
Output: Fully populated typography.dart, radius.dart, and shadows.dart with TDD tests.
</objective>

<execution_context>
@/Users/nicolasdemari/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nicolasdemari/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-token-system-theme-integration/02-CONTEXT.md
@.planning/phases/02-token-system-theme-integration/02-RESEARCH.md
@.planning/phases/02-token-system-theme-integration/02-VALIDATION.md

<interfaces>
<!-- Existing barrel export pattern from Phase 1 -->
From lib/tailwind_flutter.dart:
```dart
// ----- Tokens (Tier 1) -----
// export 'src/tokens/typography.dart';
// export 'src/tokens/radius.dart';
// export 'src/tokens/shadows.dart';
```
<!-- Uncomment these exports when files are implemented -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement TwFontSize extension type, TwFontSizes, TwFontWeights, TwLetterSpacing, TwLineHeights</name>
  <files>lib/src/tokens/typography.dart, test/src/tokens/typography_test.dart</files>
  <behavior>
    - Test: TwFontSizes has 13 entries (xs through xl9)
    - Test: TwFontSizes.xs.value == 12.0, TwFontSizes.xs.lineHeight closeTo 1.3333
    - Test: TwFontSizes.base.value == 16.0, TwFontSizes.base.lineHeight == 1.5
    - Test: TwFontSizes.xl9.value == 128.0, TwFontSizes.xl9.lineHeight == 1.0
    - Test: TwFontSizes.lg.textStyle returns TextStyle(fontSize: 18, height: ~1.5556)
    - Test: All 13 font sizes exhaustively verified (value + lineHeight)
    - Test: TwFontWeights has 9 entries (thin through black)
    - Test: TwFontWeights.thin == FontWeight.w100, TwFontWeights.bold == FontWeight.w700, TwFontWeights.black == FontWeight.w900
    - Test: All 9 font weights exhaustively verified
    - Test: TwLetterSpacing has 6 entries (tighter through widest)
    - Test: TwLetterSpacing.tighter == -0.05, TwLetterSpacing.normal == 0.0, TwLetterSpacing.widest == 0.1
    - Test: All 6 letter spacing values exhaustively verified
    - Test: TwLineHeights has 6 entries (none through loose)
    - Test: TwLineHeights.none == 1.0, TwLineHeights.normal == 1.5, TwLineHeights.loose == 2.0
    - Test: All 6 line height values exhaustively verified
  </behavior>
  <action>
    Create TwFontSize extension type wrapping a record of (size, lineHeight):

    ```dart
    extension type const TwFontSize._(({double size, double lineHeight}) _) {
      const TwFontSize(double size, double lineHeight)
          : this._((size: size, lineHeight: lineHeight));
      double get value => _.size;
      double get lineHeight => _.lineHeight;
      TextStyle get textStyle => TextStyle(fontSize: _.size, height: _.lineHeight);
    }
    ```

    Create four abstract final classes:
    1. `TwFontSizes` -- 13 static const TwFontSize members (xs through xl9) per RESEARCH.md values
    2. `TwFontWeights` -- 9 static const FontWeight members (thin=w100 through black=w900)
    3. `TwLetterSpacing` -- 6 static const double members (tighter=-0.05 through widest=0.1). Store as em multipliers, add dartdoc explaining users multiply by font size.
    4. `TwLineHeights` -- 6 static const double members (none=1.0, tight=1.25, snug=1.375, normal=1.5, relaxed=1.625, loose=2.0)

    Import `package:flutter/widgets.dart` for TextStyle and FontWeight. Do NOT import material.dart.

    Add dartdoc on each class and on TwFontSize explaining that `.textStyle` creates a TextStyle with both fontSize and height (the Tailwind "text-lg implies size + line-height" pattern).
  </action>
  <verify>
    <automated>cd /Users/nicolasdemari/dev/tailwind_flutter && flutter test test/src/tokens/typography_test.dart --no-pub</automated>
  </verify>
  <done>13 font sizes with paired line-heights, 9 font weights, 6 letter spacing values, and 6 line height values all accessible as const values. TwFontSize.textStyle returns a TextStyle with both size and height. All values exhaustively tested.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement TwRadius extension type with BorderRadius getters and TwShadows presets</name>
  <files>lib/src/tokens/radius.dart, lib/src/tokens/shadows.dart, test/src/tokens/radius_test.dart, test/src/tokens/shadows_test.dart</files>
  <behavior>
    - Test: TwRadii has 10 entries (none through full)
    - Test: TwRadii.none == 0.0, TwRadii.sm == 4.0, TwRadii.lg == 8.0, TwRadii.full == 9999.0
    - Test: All 10 radius values exhaustively verified
    - Test: TwRadius implements double -- TwRadii.lg can be used as double (value 8.0)
    - Test: TwRadii.lg.all == BorderRadius.circular(8.0)
    - Test: TwRadii.lg.top has topLeft and topRight == Radius.circular(8.0), bottomLeft and bottomRight == Radius.zero
    - Test: TwRadii.lg.bottom, .left, .right similarly verified
    - Test: TwShadows has 9 entries (xxs, xs, sm, md, lg, xl, xxl, inner, none)
    - Test: TwShadows.none is an empty list
    - Test: TwShadows.sm is a List<BoxShadow> with 2 entries (correct offset, blur, spread, color per Tailwind v4)
    - Test: TwShadows.md has correct BoxShadow values
    - Test: TwShadows.inner has a BoxShadow with inset-like positive spreadRadius (inner shadow approximation)
    - Test: All 9 shadow presets verified for structure (list length) and spot-check values
  </behavior>
  <action>
    **Radius (radius.dart):**
    Create TwRadius extension type implementing double with 5 BorderRadius getters (all, top, bottom, left, right) per the verified pattern in RESEARCH.md.

    Create `abstract final class TwRadii` with 10 static const TwRadius values: none(0), xs(2), sm(4), md(6), lg(8), xl(12), xl2(16), xl3(24), xl4(32), full(9999).

    Import `package:flutter/widgets.dart` for BorderRadius and Radius.

    **Shadows (shadows.dart):**
    Create `abstract final class TwShadows` with 9 static const List<BoxShadow> members: xxs, xs, sm, md, lg, xl, xxl, inner, none.

    No extension type wrapper per user decision -- plain `const List<BoxShadow>`.

    Map CSS shadow values from RESEARCH.md to BoxShadow constructor:
    - CSS `offset-x offset-y blur-radius spread-radius color` maps to `BoxShadow(offset: Offset(x, y), blurRadius: blur, spreadRadius: spread, color: Color.fromRGBO(0, 0, 0, opacity))`
    - Multi-shadow CSS values become multi-element Lists
    - `inner` shadow: Flutter's BoxShadow doesn't support `inset`. Approximate by using a BoxDecoration approach or document limitation. Use `BoxShadow(offset: Offset(0, 2), blurRadius: 4, spreadRadius: 0, color: Color.fromRGBO(0, 0, 0, 0.05))` and add dartdoc noting this is an inner shadow approximation (Flutter does not natively support inset shadows).
    - `none` is `const <BoxShadow>[]`

    Import `package:flutter/widgets.dart` for BoxShadow, Offset, Color.

    Add dartdoc on both classes.
  </action>
  <verify>
    <automated>cd /Users/nicolasdemari/dev/tailwind_flutter && flutter test test/src/tokens/radius_test.dart test/src/tokens/shadows_test.dart --no-pub</automated>
  </verify>
  <done>10 radius values with BorderRadius getters and 9 shadow presets as const List<BoxShadow> all accessible and tested. TwRadius implements double for transparent usage. All values exhaustively verified.</done>
</task>

</tasks>

<verification>
flutter test test/src/tokens/typography_test.dart test/src/tokens/radius_test.dart test/src/tokens/shadows_test.dart --no-pub
flutter analyze --no-pub lib/src/tokens/typography.dart lib/src/tokens/radius.dart lib/src/tokens/shadows.dart
</verification>

<success_criteria>
- 13 font sizes with paired line-heights, 9 weights, 6 letter spacings, 6 line heights all accessible
- TwFontSize.textStyle produces TextStyle with fontSize and height
- 10 radius values usable as doubles with 5 BorderRadius getters each
- 9 shadow presets as const List<BoxShadow> with correct Tailwind v4 values
- All tests pass, flutter analyze reports zero warnings
</success_criteria>

<output>
After completion, create `.planning/phases/02-token-system-theme-integration/02-02-SUMMARY.md`
</output>
