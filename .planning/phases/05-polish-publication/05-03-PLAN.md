---
phase: 05-polish-publication
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - CHANGELOG.md
  - pubspec.yaml
  - .github/workflows/ci.yml
autonomous: true
requirements:
  - INF-06
  - INF-08
must_haves:
  truths:
    - "README contains 3-step quick-start that gets a dev to first styled widget in < 5 minutes"
    - "README has Tailwind CSS comparison table mapping CSS utilities to Dart equivalents"
    - "README covers all three API tiers: tokens, extensions, composable styles"
    - "CHANGELOG has v0.1.0 entry covering all features from Phases 1-4"
    - "pubspec.yaml version is 0.1.0"
    - "CI pana threshold enforces 160/160 (--exit-code-threshold 0)"
    - "dart pub publish --dry-run reports no blocking errors"
  artifacts:
    - path: "README.md"
      provides: "Full package documentation with quick-start, API overview, comparison table"
      min_lines: 150
      contains: "Quick Start"
    - path: "CHANGELOG.md"
      provides: "v0.1.0 release entry in Keep a Changelog format"
      contains: "[0.1.0]"
    - path: "pubspec.yaml"
      provides: "Version bump to 0.1.0"
      contains: "version: 0.1.0"
    - path: ".github/workflows/ci.yml"
      provides: "Pana threshold ratcheted to 0 (enforces 160/160)"
      contains: "exit-code-threshold 0"
  key_links:
    - from: "README.md"
      to: "package:tailwind_flutter/tailwind_flutter.dart"
      via: "Code examples showing real API usage"
      pattern: "tailwind_flutter"
    - from: "CHANGELOG.md"
      to: "pubspec.yaml"
      via: "Version 0.1.0 matching pubspec"
      pattern: "0\\.1\\.0"
---

<objective>
Write the README, CHANGELOG, bump version to 0.1.0, and ratchet CI pana threshold to enforce 160/160 points.

Purpose: README (INF-06) is the primary adoption surface -- the first thing a developer sees on pub.dev. CHANGELOG (INF-08) documents release history. Version bump and CI ratchet are the mechanical steps that make publication real.

Output: Publication-ready README, CHANGELOG, pubspec with 0.1.0 version, and CI enforcing perfect pana score.
</objective>

<execution_context>
@/Users/nicolasdemari/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nicolasdemari/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-polish-publication/05-CONTEXT.md
@.planning/phases/05-polish-publication/05-RESEARCH.md

@pubspec.yaml
@README.md
@CHANGELOG.md
@.github/workflows/ci.yml
@lib/tailwind_flutter.dart

<interfaces>
<!-- Key API surface that README code examples must reference correctly. -->

Public API classes and extensions:
- TwColors (22 families: red, orange, amber, yellow, lime, green, emerald, teal, cyan, sky, blue, indigo, violet, purple, fuchsia, pink, rose, slate, gray, zinc, neutral, stone + black/white/transparent)
- TwSpacing (35 values: s0 through s96, each with .all, .x, .y EdgeInsets getters)
- TwFontSizes (13 sizes: xs through xl9, each with .value, .lineHeight, .textStyle)
- TwFontWeights (9 weights: thin through black)
- TwRadii (10 values: none through full)
- TwShadows (9 presets: xs2 through xl2 plus inner, none)
- TwOpacity (21 values: o0 through o100)
- TwBreakpoints (5 thresholds: sm, md, lg, xl, xxl)
- TwTheme (widget), TwThemeData (.light(), .dark()), context.tw accessor
- TwWidgetExtensions on Widget: .p(), .px(), .py(), .bg(), .rounded(), .shadow(), .opacity(), .width(), .height(), .center(), .align(), .clipRect(), .clipOval(), .m(), .mx(), .my(), etc.
- TwTextExtensions on Text: .bold(), .italic(), .fontSize(), .textColor(), .letterSpacing(), .lineHeight(), .fontWeight(), .textStyle()
- TwStyle: constructor, .merge(), .resolve(), .apply()
- TwVariant: .dark, .light
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write README.md with quick-start, API overview, and comparison table</name>
  <files>README.md</files>
  <action>
Complete rewrite of README.md (current content is a placeholder). Read the existing file first.

Structure per CONTEXT.md locked decisions:

1. **Header + Badges:**
   - Package name and one-line description
   - Badges: pub.dev version, CI status (link to GitHub Actions), license (MIT), style (very_good_analysis)
   - Badge ordering at Claude's discretion

2. **Features section (brief):**
   - Bullet list of key capabilities: complete Tailwind v4 token set, widget extensions, composable styles, theme integration, dark mode support

3. **Quick Start (3 steps, < 20 lines, < 5 min to first styled widget):**
   - Step 1: Add dep (`tailwind_flutter: ^0.1.0`)
   - Step 2: Wrap app in TwTheme (`TwTheme(data: TwThemeData.light(), child: ...)`)
   - Step 3: Style a widget (show chained extensions on a Text)
   - This is the adoption hook. Must be copy-pasteable and work immediately.

4. **API Overview organized by three tiers:**

   **Tier 1: Design Tokens**
   - Brief explanation: const values, type-safe, autocomplete-friendly
   - Table or examples: `TwColors.blue.shade500`, `TwSpacing.s4`, `TwFontSizes.lg.value`, `TwRadii.lg`, `TwShadows.md`
   - Mention all 7 categories

   **Tier 2: Widget Extensions**
   - Brief explanation: chain methods on any Widget (or Text)
   - Example showing `.p()`, `.bg()`, `.rounded()`, `.shadow()` chaining
   - Example showing Text extensions: `.bold()`, `.fontSize()`, `.textColor()`
   - Note: Text extensions must come before Widget extensions in chain

   **Tier 3: Composable Styles (TwStyle)**
   - Brief explanation: reusable style objects, like CSS classes
   - Example: define a TwStyle, apply to widget
   - Example: merge two styles
   - Example: variants with dark mode resolution

5. **Tailwind CSS Comparison Table:**
   Maps Tailwind CSS utilities to tailwind_flutter equivalents:
   | Tailwind CSS | tailwind_flutter |
   |---|---|
   | `bg-blue-500` | `.bg(TwColors.blue.shade500)` |
   | `p-4` | `.p(TwSpacing.s4)` |
   | `rounded-lg` | `.rounded(TwRadii.lg)` |
   | `text-lg` | `.fontSize(TwFontSizes.lg.value)` |
   | `font-bold` | `.bold()` |
   | `shadow-md` | `.shadow(TwShadows.md)` |
   | `opacity-50` | `.opacity(TwOpacity.o50)` |
   | `dark:bg-gray-800` | `TwVariant.dark` in TwStyle |
   Include 10-15 entries covering the most common utilities.

6. **Theme Setup section:**
   - Show TwTheme wrapping
   - Show TwThemeData.light() / .dark() with override example
   - Show context.tw usage for accessing tokens in build methods

7. **Contributing section:**
   - Brief, standard: file issues, fork + PR, follow lint rules
   - Not bloated

8. **License:**
   - MIT with link to LICENSE file

IMPORTANT:
- All code examples must use CORRECT API names matching the actual codebase (verify against interfaces above)
- Do NOT reference APIs that don't exist (no `TwColors.blue500` -- it's `TwColors.blue.shade500`)
- Keep it focused. pub.dev README is a marketing document, not API reference (that's what dartdoc is for)
- Use markdown that renders well on pub.dev (no HTML, no complex layouts)
  </action>
  <verify>
    <automated>cd /Users/nicolasdemari/dev/tailwind_flutter && test -f README.md && wc -l README.md | awk '{if ($1 >= 150) print "OK: " $1 " lines"; else print "FAIL: only " $1 " lines"}'</automated>
  </verify>
  <done>README.md contains quick-start (3 steps), API overview (3 tiers), Tailwind CSS comparison table, theme setup, and contributing. All code examples use correct API names.</done>
</task>

<task type="auto">
  <name>Task 2: CHANGELOG, version bump, and CI pana threshold ratchet</name>
  <files>CHANGELOG.md, pubspec.yaml, .github/workflows/ci.yml</files>
  <action>
Three surgical file updates. Read each file first before modifying.

1. **CHANGELOG.md** -- Keep a Changelog format:
   - Overwrite existing content (current content is just the initial scaffold entry)
   - Header with format attribution and SemVer adherence
   - `## [0.1.0] - 2026-03-12` entry with `### Added` section listing ALL features from Phases 1-4:
     - Complete Tailwind CSS v4 color palette (22 families x 11 shades = 242 colors) plus semantic colors
     - Spacing scale (35 values) with EdgeInsets convenience getters
     - Typography tokens: 13 font sizes, 9 weights, 6 letter spacings, 6 line heights
     - Border radius scale (10 values) with BorderRadius getters
     - Box shadow presets (7 levels plus inner and none)
     - Opacity scale and breakpoint constants
     - Theme integration: 7 ThemeExtension classes, TwTheme widget, context.tw accessor, light/dark presets
     - Widget extensions: padding, margin, background, rounded, opacity, shadow, sizing, alignment, clip
     - Text extensions: bold, italic, fontSize, textColor, letterSpacing, lineHeight, fontWeight
     - TwStyle composable styling with merge, apply, resolve, and TwVariant (light/dark)
     - Example app demonstrating all three tiers
     - Golden tests for styled widgets across light and dark themes
   - Keep the `## 0.0.1-dev.1` entry below as historical record

2. **pubspec.yaml** -- Version bump:
   - Change `version: 0.0.1-dev.1` to `version: 0.1.0`
   - Do NOT change anything else (dependencies, description, URLs, etc.)

3. **.github/workflows/ci.yml** -- Pana threshold ratchet:
   - Change `--exit-code-threshold 120` to `--exit-code-threshold 0`
   - This means ANY lost pana point will fail CI (enforces 160/160)
   - Do NOT change anything else in the CI file

IMPORTANT: These are mechanical, targeted changes. Do not restructure files or add/remove fields.
  </action>
  <verify>
    <automated>cd /Users/nicolasdemari/dev/tailwind_flutter && grep -q "version: 0.1.0" pubspec.yaml && grep -q "exit-code-threshold 0" .github/workflows/ci.yml && grep -q "\[0.1.0\]" CHANGELOG.md && echo "All three changes verified" || echo "FAIL: missing changes"</automated>
  </verify>
  <done>pubspec.yaml at 0.1.0, CI enforces 160/160, CHANGELOG has full v0.1.0 entry in Keep a Changelog format.</done>
</task>

</tasks>

<verification>
```bash
# Version check
grep "version:" /Users/nicolasdemari/dev/tailwind_flutter/pubspec.yaml
# CI threshold check
grep "exit-code-threshold" /Users/nicolasdemari/dev/tailwind_flutter/.github/workflows/ci.yml
# CHANGELOG check
head -20 /Users/nicolasdemari/dev/tailwind_flutter/CHANGELOG.md
# Publish dry-run
cd /Users/nicolasdemari/dev/tailwind_flutter && dart pub publish --dry-run
```
</verification>

<success_criteria>
- README.md has quick-start, API overview, comparison table, theme setup, contributing (>= 150 lines)
- CHANGELOG.md has [0.1.0] entry in Keep a Changelog format
- pubspec.yaml version is 0.1.0
- CI pana threshold is --exit-code-threshold 0
- `dart pub publish --dry-run` reports no blocking errors
</success_criteria>

<output>
After completion, create `.planning/phases/05-polish-publication/05-03-SUMMARY.md`
</output>
