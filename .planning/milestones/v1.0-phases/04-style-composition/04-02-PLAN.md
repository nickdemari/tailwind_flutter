---
phase: 04-style-composition
plan: 02
type: tdd
wave: 2
depends_on: ["04-01"]
files_modified:
  - lib/src/styles/tw_style.dart
  - test/src/styles/tw_style_test.dart
  - lib/tailwind_flutter.dart
  - lib/src/styles/tw_styled_widget.dart
autonomous: true
requirements: [STY-03, STY-05]

must_haves:
  truths:
    - "TwStyle.apply(child: widget) produces a widget tree in CSS box model order: margin -> constraints -> opacity -> decoration -> padding -> textStyle -> child"
    - "apply() skips widgets for null properties -- only set properties produce wrapper widgets"
    - "apply() consolidates backgroundColor, borderRadius, and shadows into a single DecoratedBox with BoxDecoration"
    - "apply() wraps textStyle in DefaultTextStyle.merge (not DefaultTextStyle constructor)"
    - "TwStyle.resolve(context) merges matching variant over base and returns flat style without variants"
    - "resolve() returns base style (without variants) when no variant matches current brightness"
    - "apply() on a style with unresolved variants uses base only (ignores variants)"
    - "Barrel export includes tw_style.dart and tw_variant.dart, does not include tw_styled_widget.dart"
  artifacts:
    - path: "lib/src/styles/tw_style.dart"
      provides: "Complete TwStyle with apply() and resolve() methods added"
      contains: "Widget apply"
      exports: ["TwStyle"]
    - path: "test/src/styles/tw_style_test.dart"
      provides: "Widget tests for apply() and resolve()"
      min_lines: 200
    - path: "lib/tailwind_flutter.dart"
      provides: "Updated barrel export with style tier uncommented"
      contains: "export 'src/styles/tw_style.dart'"
  key_links:
    - from: "lib/src/styles/tw_style.dart"
      to: "package:flutter/material.dart"
      via: "Theme.of(context).brightness for variant resolution"
      pattern: "Theme\\.of\\(context\\)\\.brightness"
    - from: "lib/src/styles/tw_style.dart"
      to: "package:flutter/widgets.dart"
      via: "Padding, DecoratedBox, Opacity, ConstrainedBox, DefaultTextStyle.merge for apply()"
      pattern: "DecoratedBox|ConstrainedBox|DefaultTextStyle\\.merge"
    - from: "lib/tailwind_flutter.dart"
      to: "lib/src/styles/tw_style.dart"
      via: "barrel export"
      pattern: "export 'src/styles/tw_style\\.dart'"
---

<objective>
Add .apply() and .resolve() to TwStyle, clean up barrel exports, and delete the tw_styled_widget.dart stub -- completing the composable styling tier.

Purpose: apply() is the rendering path that produces a CSS box model widget tree from a style object. resolve() handles dark/light variant selection. Together they enable the canonical `style.resolve(context).apply(child: widget)` two-step. Barrel export update makes TwStyle and TwVariant publicly importable.
Output: Complete TwStyle class, updated barrel export, deleted stub, all tests passing.
</objective>

<execution_context>
@/Users/nicolasdemari/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nicolasdemari/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-style-composition/04-CONTEXT.md
@.planning/phases/04-style-composition/04-RESEARCH.md
@.planning/phases/04-style-composition/04-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 output -- TwStyle and TwVariant as implemented -->

From lib/src/styles/tw_variant.dart (created in 04-01):
```dart
sealed class TwVariant {
  const TwVariant._();
  static const dark = TwDarkVariant._();
  static const light = TwLightVariant._();
  bool matches(Brightness brightness);
}
final class TwDarkVariant extends TwVariant { ... }
final class TwLightVariant extends TwVariant { ... }
```

From lib/src/styles/tw_style.dart (created in 04-01, to be extended):
```dart
@immutable
class TwStyle {
  const TwStyle({
    this.padding,    // EdgeInsets?
    this.margin,     // EdgeInsets?
    this.backgroundColor,  // Color?
    this.borderRadius,     // BorderRadius?
    this.shadows,    // List<BoxShadow>?
    this.opacity,    // double?
    this.constraints, // BoxConstraints?
    this.textStyle,  // TextStyle?
    this.variants,   // Map<TwVariant, TwStyle>?
  });

  TwStyle copyWith({...});
  TwStyle merge(TwStyle other);
  // apply() and resolve() NOT yet implemented -- this plan adds them
}
```

From lib/tailwind_flutter.dart (current barrel export):
```dart
// ----- Style Composition (Tier 3) -----
// export 'src/styles/tw_style.dart';
// export 'src/styles/tw_variant.dart';
// export 'src/styles/tw_styled_widget.dart';
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED -- Write failing tests for apply() and resolve()</name>
  <files>test/src/styles/tw_style_test.dart</files>
  <behavior>
    apply() tests (group "apply"):
    - apply with all properties set produces widget tree: Padding(margin) > ConstrainedBox > Opacity > DecoratedBox > Padding(padding) > DefaultTextStyle > child
    - apply with only padding wraps child in single Padding widget
    - apply with only backgroundColor wraps child in DecoratedBox with BoxDecoration(color: ...)
    - apply with bg + borderRadius + shadows produces single DecoratedBox with BoxDecoration containing all three
    - apply with empty style (all null) returns child unchanged
    - apply skips Opacity widget when opacity is null
    - apply skips ConstrainedBox when constraints is null
    - apply skips margin Padding when margin is null
    - apply wraps textStyle in DefaultTextStyle.merge (verify inherited style merging, not replacement)
    - apply on style WITH unresolved variants uses base properties only (variants ignored)

    resolve() tests (group "resolve"):
    - resolve in dark mode returns base merged with dark variant
    - resolve in light mode returns base merged with light variant
    - resolve returns base (without variants) when no variant matches
    - resolve with no variants map returns self (no-op)
    - resolved style has no variants (variants stripped)
    - resolve merges variant over base (variant properties override, base properties preserved where variant is null)
  </behavior>
  <action>
    Extend the existing tw_style_test.dart with two new groups: "apply" and "resolve". The apply tests use `pumpWidget` and widget tree inspection (find.byType, find.ancestor patterns). The resolve tests need a `MaterialApp` with `theme: ThemeData(brightness: ...)` wrapper to control brightness. Import `package:flutter/material.dart` for MaterialApp and ThemeData. Follow existing test patterns: use `find.ancestor` to isolate wrapper widgets from MaterialApp internals (established in Phase 3). Tests must FAIL because apply() and resolve() don't exist yet.
  </action>
  <verify>
    <automated>flutter test test/src/styles/tw_style_test.dart 2>&1 | head -10</automated>
    Tests must FAIL (compilation errors -- apply/resolve not yet implemented).
  </verify>
  <done>tw_style_test.dart has apply and resolve test groups with all behavior assertions. Compilation fails because methods don't exist on TwStyle yet.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN -- Implement apply(), resolve(), update barrel export, delete stub</name>
  <files>lib/src/styles/tw_style.dart, lib/tailwind_flutter.dart, lib/src/styles/tw_styled_widget.dart</files>
  <action>
    **tw_style.dart -- add apply() method** per RESEARCH.md Pattern 3:
    Build widget tree from child outward (innermost to outermost in code):
    1. textStyle: `DefaultTextStyle.merge(style: textStyle!, child: current)` -- NOT the DefaultTextStyle constructor
    2. padding: `Padding(padding: padding!, child: current)`
    3. decoration: `DecoratedBox(decoration: BoxDecoration(color: backgroundColor, borderRadius: borderRadius, boxShadow: shadows), child: current)` -- only if ANY of bg/borderRadius/shadows is non-null
    4. opacity: `Opacity(opacity: opacity!, child: current)`
    5. constraints: `ConstrainedBox(constraints: constraints!, child: current)`
    6. margin: `Padding(padding: margin!, child: current)`

    Method signature: `Widget apply({required Widget child})`. Returns child unchanged if all properties null. If style has variants but was not resolved, uses base properties only (variants field is simply ignored by apply -- it only reads the 8 visual fields).

    **tw_style.dart -- add resolve() method** per RESEARCH.md Pattern 5:
    - If variants is null or empty, return `this` (or `_withoutVariants()` if variants is empty map)
    - Read `Theme.of(context).brightness` (NOT MediaQuery)
    - Iterate variants entries, find first where `entry.key.matches(brightness)` is true
    - Return `_withoutVariants().merge(entry.value)` for match
    - Return `_withoutVariants()` for no match
    - Add private `TwStyle _withoutVariants()` helper that copies all 8 properties with `variants: null`

    Import `package:flutter/material.dart` for Theme.of, `package:flutter/widgets.dart` for widget types. Add dartdoc to apply() and resolve() with usage examples showing the canonical `style.resolve(context).apply(child: widget)` pattern.

    **Barrel export update (tailwind_flutter.dart):**
    - Uncomment `export 'src/styles/tw_style.dart';`
    - Uncomment `export 'src/styles/tw_variant.dart';`
    - DELETE the commented `export 'src/styles/tw_styled_widget.dart';` line entirely
    - Keep the `// ----- Style Composition (Tier 3) -----` section header

    **Delete tw_styled_widget.dart:**
    - `rm lib/src/styles/tw_styled_widget.dart` -- per CONTEXT.md decision, TwStyledWidget was dropped (not in requirements)
  </action>
  <verify>
    <automated>flutter test test/src/styles/ && flutter test && flutter analyze --no-fatal-infos</automated>
    All tests pass (style tests + full suite). Zero analyzer warnings. Barrel export works.
  </verify>
  <done>
    - apply() produces correct CSS box model widget tree for any combination of properties
    - resolve() merges matching brightness variant over base and strips variants
    - tw_styled_widget.dart deleted
    - Barrel export updated: tw_style.dart and tw_variant.dart exported, tw_styled_widget.dart line removed
    - Full test suite green, analyzer clean
  </done>
</task>

</tasks>

<verification>
- `flutter test test/src/styles/tw_style_test.dart --name "apply"` -- all apply tests pass
- `flutter test test/src/styles/tw_style_test.dart --name "resolve"` -- all resolve tests pass
- `flutter test` -- full test suite passes (no regressions)
- `flutter analyze --no-fatal-infos` -- zero warnings
- `test -f lib/src/styles/tw_styled_widget.dart && echo "FAIL: stub not deleted" || echo "OK: stub deleted"` -- stub must be gone
- `grep -c "tw_styled_widget" lib/tailwind_flutter.dart` -- must return 0 (line removed)
- `grep -c "tw_style.dart" lib/tailwind_flutter.dart` -- must return 1 (uncommented)
- `grep -c "tw_variant.dart" lib/tailwind_flutter.dart` -- must return 1 (uncommented)
</verification>

<success_criteria>
- TwStyle.apply() produces correct widget tree: margin > constraints > opacity > decoration > padding > textStyle > child
- Null properties are skipped (no unnecessary wrappers)
- bg/borderRadius/shadows consolidate into single DecoratedBox
- textStyle uses DefaultTextStyle.merge (not constructor)
- TwStyle.resolve(context) selects correct variant by brightness and returns flat style
- resolve() with no matching variant returns base without variants
- tw_styled_widget.dart deleted, barrel export updated
- Full test suite green, analyzer clean
</success_criteria>

<output>
After completion, create `.planning/phases/04-style-composition/04-02-SUMMARY.md`
</output>
