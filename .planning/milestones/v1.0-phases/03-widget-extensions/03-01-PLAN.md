---
phase: 03-widget-extensions
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - lib/src/extensions/widget_extensions.dart
  - test/src/extensions/widget_extensions_test.dart
autonomous: true
requirements: [EXT-01, EXT-02, EXT-03, EXT-04, EXT-05, EXT-06, EXT-07, EXT-08, EXT-09]

must_haves:
  truths:
    - "Any Widget can call .p(16) and produce a Padding wrapper with EdgeInsets.all(16)"
    - "Directional padding variants (.px, .py, .pt, .pb, .pl, .pr) produce correct EdgeInsets"
    - "Margin methods (.m, .mx, .my, .mt, .mb, .ml, .mr) produce Padding wrappers identical to padding variants"
    - ".bg(Color) wraps in ColoredBox with the specified color"
    - ".rounded(double) wraps in ClipRRect with BorderRadius.circular(value)"
    - ".roundedFull() wraps in ClipRRect with BorderRadius.circular(9999)"
    - ".opacity(double) wraps in Opacity widget with specified value"
    - ".shadow(List<BoxShadow>) wraps in DecoratedBox with BoxDecoration(boxShadow:)"
    - ".width(double), .height(double), .size(double, double) wrap in SizedBox with correct dimensions"
    - ".center() wraps in Center, .align(Alignment) wraps in Align"
    - ".clipRect() wraps in ClipRect, .clipOval() wraps in ClipOval"
    - "Chaining produces nested wrappers where the last call is the outermost widget"
  artifacts:
    - path: "lib/src/extensions/widget_extensions.dart"
      provides: "TwWidgetExtensions extension on Widget with all 22 methods"
      min_lines: 100
    - path: "test/src/extensions/widget_extensions_test.dart"
      provides: "Widget tests for all 22 extension methods plus chaining"
      min_lines: 150
  key_links:
    - from: "lib/src/extensions/widget_extensions.dart"
      to: "flutter/widgets.dart"
      via: "Padding, ColoredBox, ClipRRect, DecoratedBox, Opacity, SizedBox, Center, Align, ClipRect, ClipOval"
      pattern: "extension TwWidgetExtensions on Widget"
---

<objective>
Implement all Widget extension methods (padding, margin, background, radius, opacity, shadow, sizing, alignment, clip) as a single named extension on Widget, with comprehensive widget tests following TDD RED/GREEN flow.

Purpose: Enables `.p(16).bg(color).rounded(8)` chaining API that wraps widgets in single-purpose Flutter widgets, eliminating manual Container/Padding nesting.
Output: `widget_extensions.dart` with 22 methods + `widget_extensions_test.dart` with full coverage.
</objective>

<execution_context>
@/Users/nicolasdemari/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nicolasdemari/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-widget-extensions/03-CONTEXT.md
@.planning/phases/03-widget-extensions/03-RESEARCH.md

<interfaces>
<!-- Existing token types the extension methods accept as parameters -->

From lib/src/tokens/spacing.dart:
```dart
extension type const TwSpace._(double _value) implements double {
  // TwSpace implements double, so TwSpacing.s4 works directly as .p(TwSpacing.s4)
}
abstract final class TwSpacing {
  static const TwSpace s0 = TwSpace._(0);
  static const TwSpace s4 = TwSpace._(16);
  // ... 35 total values
}
```

From lib/src/tokens/radius.dart:
```dart
extension type const TwRadius._(double _value) implements double {
  // TwRadius implements double, so TwRadii.lg works directly as .rounded(TwRadii.lg)
}
abstract final class TwRadii {
  static const TwRadius none = TwRadius._(0);
  static const TwRadius lg = TwRadius._(8);
  // ... 10 total values
}
```

From lib/src/tokens/shadows.dart:
```dart
abstract final class TwShadows {
  static const List<BoxShadow> sm = [...];
  static const List<BoxShadow> md = [...];
  // ... 9 total shadow presets
}
```

From lib/src/tokens/opacity.dart:
```dart
abstract final class TwOpacity {
  static const double o0 = 0;
  static const double o50 = 0.5;
  // ... 21 total values
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED -- Write failing widget tests for all 22 extension methods</name>
  <files>test/src/extensions/widget_extensions_test.dart</files>
  <behavior>
    - .p(16) wraps child in Padding with EdgeInsets.all(16)
    - .px(16) wraps in Padding with EdgeInsets.symmetric(horizontal: 16)
    - .py(16) wraps in Padding with EdgeInsets.symmetric(vertical: 16)
    - .pt(16) wraps in Padding with EdgeInsets.only(top: 16)
    - .pb(16) wraps in Padding with EdgeInsets.only(bottom: 16)
    - .pl(16) wraps in Padding with EdgeInsets.only(left: 16)
    - .pr(16) wraps in Padding with EdgeInsets.only(right: 16)
    - .m(16) wraps in Padding with EdgeInsets.all(16) (margin = outer padding)
    - .mx(16) wraps in Padding with EdgeInsets.symmetric(horizontal: 16)
    - .my(16) wraps in Padding with EdgeInsets.symmetric(vertical: 16)
    - .mt(16) wraps in Padding with EdgeInsets.only(top: 16)
    - .mb(16) wraps in Padding with EdgeInsets.only(bottom: 16)
    - .ml(16) wraps in Padding with EdgeInsets.only(left: 16)
    - .mr(16) wraps in Padding with EdgeInsets.only(right: 16)
    - .bg(Colors.red) wraps in ColoredBox with color Colors.red
    - .rounded(8) wraps in ClipRRect with BorderRadius.circular(8)
    - .roundedFull() wraps in ClipRRect with BorderRadius.circular(9999)
    - .opacity(0.5) wraps in Opacity with opacity 0.5
    - .shadow(shadows) wraps in DecoratedBox with BoxDecoration(boxShadow: shadows)
    - .width(100) wraps in SizedBox with width 100
    - .height(100) wraps in SizedBox with height 100
    - .size(100, 200) wraps in SizedBox with width 100, height 200
    - .center() wraps in Center
    - .align(Alignment.topLeft) wraps in Align with alignment topLeft
    - .clipRect() wraps in ClipRect
    - .clipOval() wraps in ClipOval
    - Chaining .p(8).bg(Colors.blue) produces Padding child with ColoredBox parent (last call outermost)
  </behavior>
  <action>
    Create `test/src/extensions/widget_extensions_test.dart` with widget tests for all 22 methods.

    Structure test groups by requirement:
    - `group('padding extensions', ...)` -- 7 tests for .p, .px, .py, .pt, .pb, .pl, .pr (EXT-01)
    - `group('margin extensions', ...)` -- 7 tests for .m, .mx, .my, .mt, .mb, .ml, .mr (EXT-02)
    - `group('background extension', ...)` -- 1 test for .bg (EXT-03)
    - `group('border radius extensions', ...)` -- 2 tests for .rounded, .roundedFull (EXT-04)
    - `group('opacity extension', ...)` -- 1 test for .opacity (EXT-05)
    - `group('shadow extension', ...)` -- 1 test for .shadow (EXT-06)
    - `group('sizing extensions', ...)` -- 3 tests for .width, .height, .size (EXT-07)
    - `group('alignment extensions', ...)` -- 2 tests for .center, .align (EXT-08)
    - `group('clip extensions', ...)` -- 2 tests for .clipRect, .clipOval (EXT-09)
    - `group('chaining', ...)` -- 1-2 tests verifying chaining order produces correct widget tree nesting

    Each test pattern:
    ```dart
    testWidgets('.p() wraps in Padding with EdgeInsets.all', (tester) async {
      await tester.pumpWidget(
        MaterialApp(home: const SizedBox().p(16)),
      );
      final padding = tester.widget<Padding>(find.byType(Padding));
      expect(padding.padding, const EdgeInsets.all(16));
    });
    ```

    Import the extension file (which doesn't exist yet -- tests will fail to compile = RED).
    Use `import 'package:tailwind_flutter/tailwind_flutter.dart';` once barrel export is updated, but for now import the extension file directly: `import 'package:tailwind_flutter/src/extensions/widget_extensions.dart';`

    Run tests to confirm RED (compilation failure since extension methods don't exist).
    Commit: `test(03-01): add failing widget extension tests`
  </action>
  <verify>
    <automated>cd /Users/nicolasdemari/dev/tailwind_flutter && flutter test test/src/extensions/widget_extensions_test.dart 2>&1 | head -20</automated>
  </verify>
  <done>Test file exists with 27+ test cases covering all 22 methods plus chaining. Tests fail because extension methods do not exist yet (RED state).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN -- Implement TwWidgetExtensions with all 22 methods</name>
  <files>lib/src/extensions/widget_extensions.dart</files>
  <action>
    Replace the stub in `lib/src/extensions/widget_extensions.dart` with the full implementation.

    Create a single named extension `TwWidgetExtensions on Widget` containing:

    **Padding (EXT-01):** 7 methods returning `Widget`
    - `p(double value)` -- `Padding(padding: EdgeInsets.all(value), child: this)`
    - `px(double value)` -- `Padding(padding: EdgeInsets.symmetric(horizontal: value), child: this)`
    - `py(double value)` -- `Padding(padding: EdgeInsets.symmetric(vertical: value), child: this)`
    - `pt(double value)` -- `Padding(padding: EdgeInsets.only(top: value), child: this)`
    - `pb(double value)` -- `Padding(padding: EdgeInsets.only(bottom: value), child: this)`
    - `pl(double value)` -- `Padding(padding: EdgeInsets.only(left: value), child: this)`
    - `pr(double value)` -- `Padding(padding: EdgeInsets.only(right: value), child: this)`

    **Margin (EXT-02):** 7 methods returning `Widget`
    - Same pattern as padding but documented as "outer spacing (margin)" with dartdoc note: `/// **Note:** Flutter has no margin widget; margin is outer padding.`
    - `m(double value)`, `mx`, `my`, `mt`, `mb`, `ml`, `mr` -- identical Padding wrapping

    **Background (EXT-03):**
    - `bg(Color color)` -- `ColoredBox(color: color, child: this)`

    **Border radius (EXT-04):**
    - `rounded(double radius)` -- `ClipRRect(borderRadius: BorderRadius.circular(radius), child: this)`
    - `roundedFull()` -- `ClipRRect(borderRadius: BorderRadius.circular(9999), child: this)`

    **Opacity (EXT-05):**
    - `opacity(double value)` -- `Opacity(opacity: value, child: this)`

    **Shadow (EXT-06):**
    - `shadow(List<BoxShadow> shadows)` -- `DecoratedBox(decoration: BoxDecoration(boxShadow: shadows), child: this)`

    **Sizing (EXT-07):**
    - `width(double value)` -- `SizedBox(width: value, child: this)`
    - `height(double value)` -- `SizedBox(height: value, child: this)`
    - `size(double width, double height)` -- `SizedBox(width: width, height: height, child: this)`

    **Alignment (EXT-08):**
    - `center()` -- `Center(child: this)`
    - `align(Alignment alignment)` -- `Align(alignment: alignment, child: this)`

    **Clip (EXT-09):**
    - `clipRect()` -- `ClipRect(child: this)`
    - `clipOval()` -- `ClipOval(child: this)`

    **Dartdoc requirements:**
    - Every public method gets a dartdoc comment explaining what wrapper widget it produces
    - Include `/// {@example}` style usage examples showing token usage: `.p(TwSpacing.s4)`, `.bg(TwColors.blue.shade500)`
    - Add top-of-file dartdoc on the extension explaining recommended chaining order:
      `/// Inner to outer: `.p()` -> `.bg()` -> `.rounded()` -> `.shadow()` -> `.opacity()` -> `.m()`
    - Document the `.bg().rounded()` vs `.rounded().bg()` gotcha
    - On margin methods, state: "Flutter has no margin widget; margin is outer padding."

    **Import:** `import 'package:flutter/widgets.dart';` -- do NOT import material.dart (lighter import).

    Run `flutter analyze lib/src/extensions/widget_extensions.dart` -- must be zero warnings.
    Run tests -- must all pass (GREEN).
    Commit: `feat(03-01): implement TwWidgetExtensions with 22 chaining methods`
  </action>
  <verify>
    <automated>cd /Users/nicolasdemari/dev/tailwind_flutter && flutter test test/src/extensions/widget_extensions_test.dart && flutter analyze lib/src/extensions/widget_extensions.dart --no-fatal-infos</automated>
  </verify>
  <done>All 27+ widget extension tests pass. `flutter analyze` reports zero warnings on the extension file. 22 methods implemented across 9 requirement categories (EXT-01 through EXT-09).</done>
</task>

</tasks>

<verification>
- `flutter test test/src/extensions/widget_extensions_test.dart` -- all tests green
- `flutter analyze lib/src/extensions/widget_extensions.dart` -- zero warnings
- Extension methods produce correct wrapper widgets verified by widget tests
- Chaining order produces expected nesting (outermost = last call)
</verification>

<success_criteria>
- TwWidgetExtensions extension on Widget exists with 22 public methods
- Every method wraps `this` in exactly one Flutter widget (Padding, ColoredBox, ClipRRect, DecoratedBox, Opacity, SizedBox, Center, Align, ClipRect, ClipOval)
- All widget tests pass covering all 22 methods + chaining behavior
- Zero analyzer warnings
- Dartdoc on all public methods with chaining order documentation
</success_criteria>

<output>
After completion, create `.planning/phases/03-widget-extensions/03-01-SUMMARY.md`
</output>
